#taking the 1st input from the user and storing a variable
x=input("enter the no.==")
#taking the 1st input from the user and storing in another variable
y=input ("enter the no till multiplied")
#adding a for loop for the multiplication table to be continue to the input from user
for y in range(int(y)): 
	print(f"{x} x {y+1}= {int(x)*int(y+1)}")
	
